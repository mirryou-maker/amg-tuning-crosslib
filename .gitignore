# External dependencies (fetched, not committed)
ext/
data/matrices/

# Build artifacts
build/
*.o
*.exe
*.out

# Manuscript kept out of the public repo until publication (re-included then).
# The reproducibility artifact stays public, including the figure-generation
# scripts and figures under paper/figure/ that reproduce.py needs.
paper/
!paper/figure/
!paper/figure/**

# Python
__pycache__/
*.pyc

# Scratch / logs
*.tmp
tags/
