# Level-1 reproduction (figures, analysis, predictor from released data).
# A standard scientific Python stack; versions are the ones used in the paper.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Full end-to-end reproduction additionally needs (see docs/REPRODUCE.md):
#   - a C++14 compiler + Boost headers (AMGCL, header-only)
#   - a C compiler + hypre built sequentially (HYPRE_ENABLE_MPI=OFF)
#   - MPI only for cross-node work distribution on a cluster
